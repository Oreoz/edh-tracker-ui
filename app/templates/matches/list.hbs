{{!--
<button class="btn btn-outline-success" disabled={{unauthenticated}} {{action 'createMatch'}}>
  <i class="fa fa-plus-circle" aria-hidden="true"></i>
  Create Match
</button>
--}}

<div class="header-container pt-3">
  <h3>Matches</h3>
</div>

<div class="row">
  {{#each matches as |match|}}
    {{match-card class="col-md-6" match=match}}
  {{/each}}
</div>
